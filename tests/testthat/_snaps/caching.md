# Caching works

    max_size must be a number. Use `Inf` for no limit.

---

    max_size must be a number. Use `Inf` for no limit.

---

    pruning_factor must be a number between 0 and 1.

---

    pruning_factor must be a number between 0 and 1.

